# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs_24", "npm"]

[phases.install]
cmds = ["cd backend && npm install"]

[phases.build]
cmds = []

[start]
cmd = "cd backend && node test-server.js"